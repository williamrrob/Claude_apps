{
  "name": "rootwork",
  "version": "1.0.0",
  "description": "Rootwork — a visual etymology app that breaks words into their roots and affixes.",
  "private": true,
  "scripts": {
    "test": "node test/integration.test.js",
    "build:data": "node scripts/build-data.js",
    "build:rich": "node scripts/build-rich.js",
    "build:sqlite": "node --no-warnings scripts/build-sqlite.js",
    "embed": "node --no-warnings scripts/embed.js",
    "sense-merge": "node --no-warnings scripts/sense-merge.js",
    "suggest-related": "node --no-warnings scripts/suggest-related.js",
    "lint:refs": "node --no-warnings scripts/lint-refs.js",
    "find": "node --no-warnings scripts/find.js"
  },
  "devDependencies": {
    "cmu-pronouncing-dictionary": "^3.0.0",
    "playwright-core": "^1.61.1",
    "wordnet": "^2.0.0"
  }
}
